## Why
现有的 `src/content/blog/2025-10-29-openspec.md` 文章内容不完整，需要补充完整的内容以完成一篇关于 OpenSpec 使用心得的博客文章。

## What Changes
- 完成博客文章 `src/content/blog/2025-10-29-openspec.md` 的写作
- **保留现有内容**：不删除已有的引言、演进阶段和困境解决方案部分
- **文章标题**：OpenSpec 使用心得（7个字以内）
- **文章要求**：
  - 确保内容准确性：spec-kit 和 OpenSpec 的对比需准确（spec-kit 适合 greenfield/0→1，OpenSpec 更适合修改现有功能）
  - AI 工具演进阶段需准确描述
  - **精简整篇文章篇幅**：
    - 去除所有冗余和重复内容（如"共识无法复用"不应重复出现）
    - 合并相似内容的段落
    - 使用案例部分需要详细描述：保留对话示例和文件内容示例，不能忽略这些重要示例
    - 其他部分精简：除了使用案例部分外，其他部分应精简，每个段落紧扣主题，避免过渡冗余
  - **统一名词使用**：
    - 根据场景选择合适的名词（AI Agent、agent、AI 工具等）
    - 全文保持名词使用的一致性
    - 当指代具体的 AI 助手时，统一使用"AI Agent"
    - 当泛指 AI 能力时，使用"AI"
    - 当指代工具时，使用"AI 工具"
- **文章目录结构**：
  ```
  # OpenSpec 使用心得
  
  ## 引言：拥抱 AI 工具
  
  ## AI 工具的演进阶段
  
  （描述四个阶段：洪荒时代、集成时代、增强时代、智能时代）
  
  ## 现有困境与解决方案
    ### 现有流程的问题
    ### 过去的工作方式
    ### OpenSpec 的解决思路
    ### 角色的转变
  
  ## OpenSpec 使用案例
    ### 工具介绍
    ### 安装 OpenSpec
    ### 初始化 OpenSpec
    ### 完成这篇文章提案
      - 对话过程（详细示例）
      - 文件结构
      - 文件内容示例（详细）
      - 提案的价值
  
  ## 个人实践心得
    - 从 spec-kit 到 OpenSpec
    - 不需要记住指令
    - Agent 自动化
    - 降低心智负担
    - 工具可切换性
  ```
- 文章应涵盖以下五个方面（已移除"总结与展望"部分）：
  1. 2025 年开发者应积极拥抱 AI 工具（已有引言，可适当完善）
  2. 开发者使用 AI 工具的演进阶段（需完善，突出各阶段的重点）
     - 阶段1：AI 充其量只能是一个辅助的工具
     - 阶段2：省去了手动复制代码的流程
     - 阶段3：MCP 出来后，AI 可以做更多事情
     - 阶段4：Agent 出来后，它完全可以自己干活
  3. 现有流程的困境与 OpenSpec 的解决方案（需完善，包含以下要点）
     - 现有流程的问题：`.cursorrules` 不够；chat 模式会误解；每次修改都是黑盒；上下文串台；超过上下文后信息丢失；共识无法复用（此问题只在此处完整表述，不应在其他地方重复出现）
     - 过去的工作方式及其局限：先让 AI Agent 给思路，OK 了再写，但核心问题依然存在（避免重复"共识无法复用"的完整表述）
     - OpenSpec 的解决思路：每个改动都是提案，实施前完善补充，心里有底；保留的文件是可切换性和知识传承的重要资源
     - 角色的转变：从微观指导到宏观放权，工作流程是：讲需求 → AI Agent 写提案 → review → AI Agent 修改 → OK 后出活
  4. OpenSpec 的特点与使用流程 - 使用案例：完成这篇文章提案本身（需补充）
     - 先介绍 spec-kit 和 OpenSpec 工具：准确说明这两个工具的适用场景对比
       - spec-kit 的优势：适合 greenfield/0→1（从零开始的项目）
       - OpenSpec 的优势：采用两文件夹模型（specs/ 存放当前规范，changes/ 存放提案），更适合修改现有功能或涉及多个 spec 的场景，可扩展性更好
     - OpenSpec 的安装方法：
       - 检查 Node.js 版本（需要 >= 20.19.0）
       - 全局安装：`npm install -g @fission-ai/openspec@latest`
       - 验证安装：`openspec --version`
     - OpenSpec 的初始化方法：
       - 进入项目目录
       - 运行 `openspec init`
       - 说明初始化过程中会发生什么（选择 AI 工具、配置 slash commands、创建 openspec/ 目录结构）
       - 初始化后的验证：`openspec list`
     - 参考 https://github.com/Fission-AI/OpenSpec 获取准确信息
     - 使用案例：完成这篇文章提案本身（精简描述，突出核心流程）
  5. 个人实践心得（需补充，包含以下要点）
     - 先接触 spec-kit，但流程比较繁琐，看不懂
     - 转向更轻量的 OpenSpec，觉得更适用于自己
     - 不想花心思记住 OpenSpec 的指令，只需要记住如何正确初始化 OpenSpec
     - 之后可以让 AI Agent 自动根据 OpenSpec 规范使用对应指令（如创建提案、验证提案等）
     - 只需要关注提案内容是否与需求一致，不需要关心具体的指令
     - 这样在切换使用时没有心智负担，最重要的是流程和规范
     - 工具选择变得不重要，因为只要 OpenSpec 相关文件还在，可以随时切换成别的 AI Agent 工具
  3. 现有流程的困境与 OpenSpec 的解决方案（需完善，包含以下要点）
     - 现有流程的问题：`.cursorrules` 不够；chat 模式我们会误导，AI Agent 会误解；每次修改都是黑盒；上下文串台；超过上下文后信息丢失；共识无法复用（此问题只在此处完整表述，不应在其他地方重复出现）
     - 过去的工作方式及其局限：先让 AI Agent 给思路，OK 了再写，但核心问题依然存在（避免重复"共识无法复用"的完整表述）
     - OpenSpec 的解决思路：每个改动都是提案，实施前完善补充，心里有底；保留的文件是可切换性和知识传承的重要资源
     - 角色的转变：从微观指导到宏观放权，工作流程是：讲需求 → AI Agent 写提案 → review → AI Agent 修改 → OK 后出活
  4. OpenSpec 的特点与使用流程 - 使用案例：完成这篇文章提案本身（需补充）
     - 先介绍 spec-kit 和 OpenSpec 工具：准确说明这两个工具的适用场景对比
       - spec-kit 的优势：适合 greenfield/0→1（从零开始的项目）
       - OpenSpec 的优势：采用两文件夹模型（specs/ 存放当前规范，changes/ 存放提案），更适合修改现有功能或涉及多个 spec 的场景，可扩展性更好
       - 注意：不要说是"早期"工具，避免误导
     - OpenSpec 的安装方法：
       - 检查 Node.js 版本（需要 >= 20.19.0）
       - 全局安装：`npm install -g @fission-ai/openspec@latest`
       - 验证安装：`openspec --version`
     - OpenSpec 的初始化方法：
       - 进入项目目录
       - 运行 `openspec init`
       - 说明初始化过程中会发生什么（选择 AI 工具、配置 slash commands、创建 openspec/ 目录结构）
       - 初始化后的验证：`openspec list`
     - 参考 https://github.com/Fission-AI/OpenSpec 获取准确信息
     - 使用案例：完成这篇文章提案本身（详细描述，不能忽略对话示例和文件内容）
       - 详细描述对话过程：从提出需求到完善提案的完整对话示例
       - 展示 OpenSpec 产生的文件结构（proposal.md、tasks.md、specs/blog-content/spec.md）
       - 详细展示各文件的大概内容示例，说明每个文件的作用
       - 强调提案的价值：共识可持久化、知识可传承
  5. 个人实践心得（需补充，包含以下要点）
     - 先接触 spec-kit，但流程比较繁琐，看不懂
     - 转向更轻量的 OpenSpec，觉得更适用于自己
     - 不想花心思记住 OpenSpec 的指令，只需要记住如何正确初始化 OpenSpec
     - 之后可以让 AI Agent 自动根据 OpenSpec 规范使用对应指令（如创建提案、验证提案等）
     - 只需要关注提案内容是否与需求一致，不需要关心具体的指令
     - 这样在切换使用时没有心智负担，最重要的是流程和规范
     - 工具选择变得不重要，因为只要 OpenSpec 相关文件还在，可以随时切换成别的 AI Agent 工具

## Impact
- 受影响文件：`src/content/blog/2025-10-29-openspec.md`
- 这是内容创作任务，不影响代码功能

